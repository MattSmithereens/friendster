import React, { Component } from 'react';
import "./Snap.css"

const Snap = () => {
  return (<div className="Snap">
  <p>
  Before advancing, read the first two steps (Break the UI Into a Component Hierarchy and Build a Static Version in React) in the article that discuss how to mock-up a React application, break its UI into a component hierarchy, and build a static version. This is a really important skill to learn.

Don't assume React components are the exact same as the Angular components you've recently grown comfortable with, despite a similar name. React components are actually quite different; they represent UI containers, not points of functionality. Take your time. Question your preconceived notions of what components should be.
  </p></div>);
};

export default Snap;